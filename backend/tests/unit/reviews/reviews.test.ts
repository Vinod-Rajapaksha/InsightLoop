import { ReviewAction } from '../../../src/models/ReviewAction';

describe('ReviewAction Model - Unit Tests', () => {
  it('should be defined', () => {
    expect(ReviewAction).toBeDefined();
  });
});
